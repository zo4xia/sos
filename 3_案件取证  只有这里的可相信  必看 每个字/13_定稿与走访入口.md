# 定稿与实地走访入口

## 定稿结论

甲方业务主线已经收口。后续不再用“先随便选一个版本再补功能”的方式推进，而是先以甲方定盘事实建立验真清单，再对原项目实地取证。

## 走访原则

- 原项目 `3`：只读调查，不改业务代码。
- `3_clean`：污染现场，只记录其变更，不当作基线。
- 项目内 Markdown：历史口供，只有与甲方事实一致且有代码证据时才可采信。
- 甲方 DOCX 与直接证言：业务裁决依据。

## 第一轮实地走访顺序

1. 现场封存：目录、Git 状态、分支、未跟踪文件、两个 web 版本和运行入口。
2. 身份底座：登录入口、账号来源、归属地绑定、角色产生、Token、后端授权。
3. 污染追踪：`voter`、`user`、`admin`、`role`、`orgId` 及审核/投票状态的定义和全调用链。
4. 主链实现：提案状态机、通过触发、届次活动、归档、D 日 Pipeline、公告生成。
5. 候选人链：自荐/组织推荐、材料审核、按届列表、线下送审、结果回填、小程序同步。
6. 文件链：多附件存储、归档路径、预览、整套下载、外部送审附件包。

## 走访产出

每条结论必须记录：证据路径、代码位置、实际行为、甲方对应条目、冲突、风险和处理建议。没有闭环的内容保持“待核验”，不写成“完成”。

## 第一轮现场记录（2026-09-03）

### 现场结构

- jCodeMunch 已索引原项目 `/mnt/c/Users/zo4xi/Desktop/3`，统计为 722 个文件、3,614 个符号。
- 现场至少存在根 `backend`、`backend/backend`、根 `web`、`backend/web`、`gml` 五类重叠或平行内容，并且 `web/.codebuddy/backup-2026-09-03` 存在同日备份。
- `backend` 与 `miniprogram` 各有一份，`backend/web` 与 `web` 也各有一份，存在版本漂移风险。
- jCodeMunch 健康快照：等级 `D`，4 个依赖环，176 个不稳定模块，约 6.4% 潜在死代码；测试证据轴为 0，不能据此确认业务可运行。
- `get_project_intel` 未发现可解析的 Docker、CI、环境变量、依赖脚本或 API 元数据；可能是扫描范围/索引解析限制，暂不等同于项目没有这些文件。
- Git 命令在当前挂载路径 `/mnt/c/Users/zo4xi/Desktop/3` 返回“not a git repository”，真实仓库位置、挂载映射或现场是否脱离 Git 待核验。

### 高危概念污染硬证据

以下均来自 jCodeMunch 对原项目的只读搜索，代码位置以仓库相对路径和行号为证据句柄：

| 证据位置 | 实际行为 | 与甲方规则的冲突 |
|---|---|---|
| `backend/api.js:150-152`；重复于 `backend/backend/api.js` | 公开注册流程向 `accounts` 写入 `roles='voter'`，返回用户 `role='user'`、`roles='voter'` | 甲方明确不做公众注册、选民账号，参选人不得与选民混用 |
| `backend/miniprogram/pages/login/login.js:116,185,220,287-289`；重复于根 `miniprogram` | 未注册或游客兜底映射到 `voters`，并构造游客身份进入小程序 | 形成公众/游客账号入口，违反后台不得开放外人注册及 C 端投票模型隔离 |
| `backend/miniprogram/data/db.js:2084-2088,2327-2370,2490,5557` | 种子/数据定义仍有 `voters` 角色、选民投票权限、`voters` 数据表及按选举过滤 | 选民和投票仍是系统数据模型的一等对象，而非已废弃的历史隔离数据 |
| `backend/scripts/01_inspect.js:61-64`、`02_inspect_compact.js:49-51`、`08_legacy.js:6-7`；重复于 `backend/backend/scripts` | 检查脚本持续统计 `voters` 表、选民数和选民与结果对照 | 历史运维/验收脚本会继续把错误概念当作正常业务基线 |
| `backend/api.js:720-725,772-776`；重复于 `backend/backend/api.js` | 候选人状态派生接收 `votes`，结果接口写入候选人票数和状态 | 甲方只允许后台回填线下结果，不代表系统建立线上投票/选民管理；字段和状态需重新定义并审查授权 |
| `backend/miniprogram/pages/notice/notice.js:194` | 公示层展示“参选率/实际投票人数” | 可能属于甲方不要求的选民统计展示，需以 DOCX 逐条对照后删除或隔离 |

### 当前裁决

这是“多版本现场 + 身份权限污染 + 选民投票污染”的高危组合。暂不修补任何业务页面，不从现有字段名推导真实模型。下一轮只读定点检查认证实现、Token 生命周期、每个后端路由的授权中间件和 `orgId` 过滤。

### 地基判断纠偏（2026-09-03）

先前依据 jCodeMunch 索引把 `gml` 判断为候选地基，现撤回。复核发现：索引可以返回 `gml` 的符号和源码片段，但同一索引的 `get_project_intel` 返回 `file_count: 0`，全文检索返回 `gml` 零结果，且现场目录存在明显的重复映射路径。由此只能证明索引中有 `gml` 记录，不能证明当前工作区有可落地的 `gml` 源码。

甲方/探案现场的实际观察优先。当前裁决改为：**尚未选择地基**。在确认物理目录、可读源码、依赖安装、启动入口和可重复构建前，任何版本都只能作为证据现场，不能作为施工地基。

后续先做“现场与索引一致性核验”，再在真实存在的版本中选择单一落脚点；不因索引残留继续推进 `gml`。

### 污染责任判断边界（2026-09-03）

当前证据足以确认：项目曾被多次复制、拼接或错误改造，形成版本漂移、路径重复、索引残留、`voter`/游客注册/投票统计等不符合甲方规则的污染。可以称为“庸医式施工”或“严重误改”。

当前证据不足以确认：具体操作者、具体时间线、是否主观故意“下黑手”。在取得 Git 提交、文件时间线、代理对话原文和真实工作区校验前，不把技术污染直接定性为恶意破坏。

### 小程序端基线裁决（2026-09-03）

甲方确认采用 `C:\\Users\\zo4xi\\Desktop\\3\\miniprogram` 作为小程序端前端基线。该目录的页面结构、视觉设计和交互形态获得甲方认可，后续小程序界面施工以此为参考。

该确认不等同于业务逻辑验收：登录身份、游客兜底、接口调用、数据映射、权限校验、审核状态和投票相关逻辑均保持待核验。`backend/miniprogram` 不与根目录版本自动合并，仅作为差异和历史证据对照。

### 第一案发现场裁决（2026-09-03）

为缩短调查路径，甲方确认 `C:\\Users\\zo4xi\\Desktop\\3\\backend` 为案情事件的**第一案发现场**。认证、授权、组织隔离、数据接口、脚本及后端运行入口的取证和后续修复，均优先以该目录为调查入口。

该裁决是现场优先级，不是代码质量或业务正确性背书。默认核验对象为 `backend/api.js`、`backend/lib/`、`backend/scripts/`、`backend/package.json` 及直接依赖；`backend/backend` 是已确认存在版本漂移的嵌套副本，仅保留为历史差异证据，禁止自动作为修改目标或与主现场混合。

### 现场入口迁移（2026-09-03）

甲方将当前案情现场迁移至 `K:\\3\\3\\`。在当前 Linux 运行环境中，该路径对应挂载点 `/mnt/k/3/3`，已确认物理可读。现场包含 `backend/`、`miniprogram/`、`web/`、案件卷宗、备份文件、运行日志、依赖目录及数据库修复材料。

此前 jCodeMunch 登记的 `/mnt/c/Users/zo4xi/Desktop/3` 仅作为旧现场索引和历史参考；截至本记录，K 盘现场尚未建立独立代码图谱索引，不假定新旧现场内容一致。后续取证以 `/mnt/k/3/3` 为准，涉及密钥、日志、数据库和修复脚本的内容先只读封存。

### 迁移后闭环验证（2026-09-03）

- 现场验收：`/mnt/k/3/3`、`backend/`、`miniprogram/`、`web/` 及本卷宗记录均可读，确认 K 盘现场迁移实际生效。
- 后端边界：`backend/api.js` 通过 `node --check`；`backend/package.json` 声明 `start` 入口为 `node api.js`。该包未声明测试脚本，执行 `npm test` 明确失败，不得写成“测试通过”。
- Web 边界：`npm run typecheck` 通过；真实 `npm run build` 在 Vite/Rollup 阶段失败，现有依赖缺少平台包 `@rollup/rollup-linux-x64-gnu`。因此 Web 当前只能确认类型检查通过，不能确认生产构建成功。
- 小程序边界：根目录 `miniprogram` 的关键前端文件和 `package-lock.json` 存在，但没有 `node_modules`，且仅有占位测试脚本；其页面基线可读，依赖安装和业务运行仍待核验。
- 版本边界：`/mnt/k/3/3` 不是 Git 仓库，当前无法从该目录确认提交历史、分支和责任时间线。

本轮闭环结论：**现场迁移已被实际路径和端侧检查确认；工程可直接构建运行尚未成立。** 后续若要修复构建，只能在保留现场副本后单独补齐依赖并重新执行真实构建，不能把本轮失败绕写成完成。
