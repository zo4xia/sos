# 前端页面功能模块 ↔ 后端数据库表 对照表（脱稿制）

> **生成时间**：2026-09-04
> **真相来源**：web/src/pages 逐页代码提取 + 本地 PostgreSQL backend_new 真实 schema（22表）
> **用途**：一眼看清每个页面有什么功能、对应哪张表、哪些接口404、哪些是假数据

---

## 一、总览：22张数据库表

| 表名 | 用途 | 记录数 |
|---|---|---|
| organizations | 归属地（村/社区） | 2（演示村+演示社区） |
| users | 用户（后台+参选人） | 11 |
| memberships | 用户-归属地-角色关联 | 11 |
| sessions | 登录会话 | 动态 |
| invitations | 邀请码 | 0 |
| roles | 角色定义 | 5 |
| role_permissions | 角色-权限点 | 44 |
| election_terms | 届次 | 2 |
| election_units | 选举单位 | 2 |
| election_fiefs | 选举封地（活动） | 4 |
| election_fief_stages | 封地阶段日程 | 56 |
| stage_templates | 阶段模板（村/居各14） | 28 |
| election_proposals | 选举提案 | 3 |
| positions | 岗位 | 9 |
| candidates | 候选人 | 3 |
| candidate_reviews | 候选人轮次审核 | 4 |
| materials | 报名材料 | 动态 |
| material_files | 材料附件 | 动态 |
| announcement_templates | 公告模板 | 19 |
| announcements | 公告 | 51 |
| webhook_subscriptions | 通知订阅 | 0 |

---

## 二、逐页对照

### 1. 首页工作台 `/election/home`

| 功能模块 | 前端实现 | 调用接口 | 数据库表 | 状态 |
|---|---|---|---|---|
| D日倒计时 | 距D日天数 | /api/elections | election_fiefs.d_day | ✅ |
| 正式选举日 | 日期显示 | /api/elections | election_fiefs.d_day | ✅ |
| 法定阶段进度 | 进度条0% | /api/elections/:id/stages | election_fief_stages | ⚠️ 始终0% |
| 已发布公告 | 列表/空态 | /api/announcements | announcements(status=published) | ✅ 空态正确（全是draft） |
| 下一法定阶段 | 空态 | /api/elections/:id/stages | election_fief_stages | ❌ 应按日期计算当前/下一阶段 |

**问题**：活动名"2026年村居换届换届选举"换届重复；下一阶段不计算。

---

### 2. 提案列表 `/election/proposals`

| 功能模块 | 前端实现 | 调用接口 | 数据库表 | 状态 |
|---|---|---|---|---|
| 提案列表 | 提交时间/名称/选举方式/状态 | GET /admin/proposals | election_proposals | ✅ 3条 |
| 筛选 | 名称/选举方式/状态 | 前端过滤 | — | ✅ |
| 创建提案 | 名称/选举方式/D日/岗位列表/附件 | POST /admin/proposals | election_proposals, positions | ✅ |
| 编辑提案 | 修改提案 | PUT /api/proposals/:id | election_proposals | ❌ 旧路径404 |
| 审核通过 | 带备注→自动生成封地+日程+岗位+公告 | POST /admin/proposals/:id/review | 全表pipeline | ✅ 27/27测试 |
| 审核驳回 | 带备注 | POST /admin/proposals/:id/review | election_proposals | ✅ |

**问题**：updateProposal 用旧 /api/proposals/:id 路径，后端无此接口。

---

### 3. 活动列表 `/election/activities`

| 功能模块 | 前端实现 | 调用接口 | 数据库表 | 状态 |
|---|---|---|---|---|
| 活动列表 | 届次/名称/选举方式/D日/岗位/状态 | /api/elections | election_fiefs, election_terms | ✅ 4条 |
| 筛选 | 届次/年份/状态 | 前端过滤 | — | ✅ |
| 新建活动 | 按钮提示"由提案自动生成" | — | — | ✅ |
| 查看本届 | 跳转活动详情 | — | — | ✅ |

**问题**：标题"活动列表（）"括号为空。

---

### 4. 活动详情 `/election/activity/:id`

| 功能模块 | 前端实现 | 调用接口 | 数据库表 | 状态 |
|---|---|---|---|---|
| 公告编辑 | 标题/正文/落款/成文日期 | noticeStore（本地） | announcements | ⚠️ 用本地store非后端 |
| 发布方式 | 立即/定时 | publishNotice（本地） | announcements | ⚠️ |
| 定时发布 | 日期时间选择 | publishNotice | announcements.scheduled_for | ⚠️ |
| 公告预览 | 弹窗预览 | 本地 | — | ✅ |
| 岗位附件上传 | 上传文件 | POST /api/positions/:id/file ❌ | material_files | ❌ 旧路径404 |
| 跳转材料/候选人 | 按钮导航 | — | — | ✅ |

**问题**：用本地 noticeStore 而非后端实时数据；发布接口未映射到 /admin/announcements/:id/publish。

---

### 5. 岗位选举表 `/election/positions`

| 功能模块 | 前端实现 | 调用接口 | 数据库表 | 状态 |
|---|---|---|---|---|
| 岗位列表 | 届名/选举方式/D日/岗位需求 | /api/positions | positions, election_fiefs | ✅ 4条 |
| 岗位明细 | 弹窗：岗位/人数/任职要求 | /api/positions | positions | ✅ |
| 岗位附件上传 | 上传文件 | POST /api/positions/:id/file ❌ | material_files | ❌ 旧路径404 |

**问题**：附件上传用旧路径。

---

### 6. 材料记录 `/election/materials`

| 功能模块 | 前端实现 | 调用接口 | 数据库表 | 状态 |
|---|---|---|---|---|
| 材料列表 | 提交人/手机号/类型/岗位/附件/时间/状态 | GET /admin/materials | materials, users | ✅ |
| 筛选 | 状态/类型/关键词 | 前端过滤 | — | ✅ |
| 材料详情 | 附件预览/下载 | /admin/materials | material_files | ✅ |
| 审核通过 | 一键通过 | PATCH /admin/materials/:id/review | materials | ✅ |
| 审核驳回 | 带备注 | PATCH /admin/materials/:id/review | materials | ✅ |
| 内推新增 | 姓名/手机号/岗位/备注/附件 | POST /admin/materials + /admin/materials/:id/file | materials, material_files, users | ✅ |

**状态**：全部正常，是最完整的页面。

---

### 7. 候选人审核 `/election/candidates`

| 功能模块 | 前端实现 | 调用接口 | 数据库表 | 状态 |
|---|---|---|---|---|
| 活动列表 | 届次/活动名/D日/候选人数/状态 | /api/elections + /api/candidates | election_fiefs, candidates | ✅ |
| 候选人列表 | 姓名/性别年龄/来源/岗位/四轮/状态 | /api/candidates | candidates, users | ✅ 3条 |
| 筛选 | 状态/来源/关键词 | 前端过滤 | — | ✅ |
| 四轮审核详情 | 每轮通过/不通过+理由+审核人 | /api/candidates（reviews平铺） | candidate_reviews | ✅ |
| 录入轮次结果 | 通过/不通过+理由 | POST /admin/candidates/:id/reviews | candidate_reviews, candidates | ✅ |
| 附件预览下载 | 材料附件 | /api/candidates（materials内嵌） | material_files | ✅ |

**问题**：第一个活动候选人数显示0（候选人绑定的fief_id与列表不匹配）；已删除投票/得票/送审邮件功能。

---

### 8. 公告记录 `/election/announcements`

| 功能模块 | 前端实现 | 调用接口 | 数据库表 | 状态 |
|---|---|---|---|---|
| 届次列表 | 届次/活动名/D日/阶段数/公告数 | /api/elections + /api/announcements | election_fiefs, announcements | ✅ |
| 公告列表 | 编号/标题/所属阶段/状态/发布时间/附件 | /api/announcements + /api/elections/:id/stages | announcements, election_fief_stages | ✅ 51条 |
| 查看列表 | 切换届次 | — | — | ✅ |

**状态**：全部正常。

---

### 9. 快捷模板 `/election/quick-templates`

| 功能模块 | 前端实现 | 调用接口 | 数据库表 | 状态 |
|---|---|---|---|---|
| 模板列表 | 18套公文（村委会/居委会版） | 本地静态 announcementTemplates.ts | announcement_templates | ⚠️ 用本地非后端 |
| 版本切换 | 村委会版/居委会版 | 本地 | — | ✅ |
| 模板搜索 | 编号/标题搜索 | 本地 | — | ✅ |
| 复制全文 | 复制按钮 | 本地 | — | ✅ |
| 跳转编辑 | 去公告记录编辑 | — | — | ✅ |

**问题**：应从后端 GET /admin/announcement-templates 读取（后端已有此接口）；模板占位符【届】【选举日】未自动填充。

---

### 10. 历史归档 `/election/archives`

| 功能模块 | 前端实现 | 调用接口 | 数据库表 | 状态 |
|---|---|---|---|---|
| 归档目录树 | 按届/提案/公告/材料/结果分类 | GET /api/archives ❌ | 无对应表 | ❌ 404 |
| 归档详情 | 弹窗 | /api/archives ❌ | — | ❌ |

**问题**：后端无归档表和接口，归档功能完全未实现。

---

### 11. 人员管理 `/admin/users`

| 功能模块 | 前端实现 | 调用接口 | 数据库表 | 状态 |
|---|---|---|---|---|
| 账号列表 | 姓名/手机号/归属地/角色/状态/创建时间 | GET /api/admin/accounts ❌ | users, memberships, organizations | ❌ 404 |
| 归属地筛选 | 下拉筛选 | /api/admin/accounts ❌ | — | ❌ |
| 重置密码 | 弹窗确认 | PUT /api/admin/accounts/:id/reset-password ❌ | users.password_hash | ❌ |
| 启用/停用 | 切换状态 | PUT /api/admin/accounts/:id/status ❌ | users.status | ❌ |
| 账号预设 | 跳转/org-setup | — | — | ✅ |

**问题**：全部接口404。后端有 POST /admin/accounts（创建）和 GET /admin/organizations，但没有 GET 账号列表、重置密码、启用停用接口。

---

### 12. 角色管理 `/admin/roles`

| 功能模块 | 前端实现 | 调用接口 | 数据库表 | 状态 |
|---|---|---|---|---|
| 角色列表 | 角色名/编码/类型/权限点 | GET /admin/roles + /admin/role-permissions | roles, role_permissions | ⚠️ |

**问题**：接口存在，但需要 role:manage 权限，仅 platform_admin 可访问；sub_admin 访问返回 unauthorized。

---

### 13. 短信配置 `/admin/sms`

| 功能模块 | 前端实现 | 调用接口 | 数据库表 | 状态 |
|---|---|---|---|---|
| 占位页 | Placeholder组件 | 无 | 无 | ❌ 白屏显示404 JSON |

**问题**：纯占位页，无实际功能。

---

### 14. 通知管理 `/admin/notifications`

| 功能模块 | 前端实现 | 调用接口 | 数据库表 | 状态 |
|---|---|---|---|---|
| 订阅列表 | 名称/渠道/URL/手机号/状态 | GET /admin/webhook-subscriptions | webhook_subscriptions | ⚠️ 接口存在但页面404 |
| 创建订阅 | 名称/渠道/URL/手机号 | POST /admin/webhook-subscriptions | webhook_subscriptions | ⚠️ |
| 编辑订阅 | 修改 | PATCH /admin/webhook-subscriptions/:id | webhook_subscriptions | ⚠️ |
| 删除订阅 | 删除 | DELETE /admin/webhook-subscriptions/:id | webhook_subscriptions | ⚠️ |
| 启用/禁用 | Switch切换 | PATCH /admin/webhook-subscriptions/:id | webhook_subscriptions | ⚠️ |
| 测试推送 | 测试按钮 | POST /admin/webhook-subscriptions/:id/test | — | ⚠️ |

**问题**：页面显示404，但代码调用的是正确的 /admin/webhook-subscriptions 接口。可能是页面组件渲染问题或路由问题。

---

### 15. 归属地账号预设 `/org-setup`（隐藏页）

| 功能模块 | 前端实现 | 调用接口 | 数据库表 | 状态 |
|---|---|---|---|---|
| 批量预设账号 | 归属地/角色/手机号/密码 | POST /api/admin/accounts ❌ | users, memberships | ❌ 旧路径 |

**问题**：用旧 /api/admin/accounts 路径，后端是 POST /admin/accounts。

---

## 三、接口404/错误汇总

| 前端调用 | 后端实际 | 修复方案 |
|---|---|---|
| GET /api/archives | 不存在 | 后端补接口或前端改空态 |
| GET /api/admin/accounts | 不存在 | 后端补 GET /admin/accounts 列表接口 |
| PUT /api/admin/accounts/:id/reset-password | 不存在 | 后端补重置密码接口 |
| PUT /api/admin/accounts/:id/status | 不存在 | 后端补启用停用接口 |
| PUT /api/proposals/:id | 不存在 | 后端补 PATCH /admin/proposals/:id |
| POST /api/positions/:id/file | 不存在 | 后端补或复用 /admin/materials/:id/file |
| POST /api/announcements/:id/file | 不存在 | 后端补公告附件接口 |
| POST /api/proposals/:id/file | 不存在 | 后端补提案附件接口 |
| GET /admin/notifications | 不存在（页面404） | 页面已用 /admin/webhook-subscriptions，需排查渲染 |
| PUT /api/announcements/:id/publish | POST /admin/announcements/:id/publish | ✅ 已修复 |
| PUT /api/candidates/:id/result | 已删除（投票污染） | ✅ 已删除 |
| PUT /api/candidates/:id/round | POST /admin/candidates/:id/reviews | ✅ 已修复 |

---

## 四、假数据/本地状态残留

| 位置 | 问题 | 修复方案 |
|---|---|---|
| noticeStore.ts | 公告用本地状态而非后端 | 改为调用 /admin/announcements |
| liveSync.ts | 失败时保持本地兜底 | 删除兜底，失败显示空态 |
| roleStore.ts | localStorage 切角色 | 权限从后端 /auth/permissions 获取 |
| chart.ts | 硬编码图表数据 | 从后端接口取数 |
| main.tsx | 登录失败mock兜底 | 删除 |
| QuickTemplates | 本地静态模板数据 | 改为 GET /admin/announcement-templates |
| ActivityDetail | 公告编辑用本地noticeStore | 改为后端实时数据 |

---

## 五、修复优先级

**P0（阻断使用）**：
1. /admin/users 人员管理全部404 → 后端补账号列表/重置密码/启用停用接口
2. /admin/notifications 页面404 → 排查页面渲染
3. /api/archives 404 → 后端补接口或改空态
4. sub_admin 隐藏无权限的"后台管理"菜单

**P1（功能不完整）**：
5. ActivityDetail 公告编辑改用后端实时数据
6. updateProposal 改到 /admin/proposals/:id
7. 岗位/公告/提案附件上传接口
8. QuickTemplates 改从后端读取模板

**P2（体验优化）**：
9. 活动名"换届换届"去重
10. 首页下一法定阶段按stages计算
11. 候选人fief_id对齐
12. 短信配置页改为真实功能或移除菜单
